node_modules/
test-results/
playwright-report/
blob-report/
graphify-out/
*.log

# O arquivo abaixo não existe hoje. A regra fica porque é o nome do bundle de
# ~2,5 MB que o canvas da marca gera ao ser exportado: se você reexportar,
# ele continua fora do repositório. Os artboards .dc.html é que são a fonte.
design-system/marca-canvas/marca-toni-coimbra.html

# Bundles exportados do design system: artefato local, não entra no repo.
*.zip

# Catálogo 3D legado, restaurado do histórico para consulta: depende de CDN
# (Tailwind, three.js) e não faz parte do que o site publica.
catalogo-3d-legado.html

# Configuração de editor: não pertence ao repositório nem à imagem.
.vscode
